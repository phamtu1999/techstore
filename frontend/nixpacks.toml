[phases.setup]
nixPkgs = ["...", "nginx"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "sed -i 's/listen 8080;/listen '\"$PORT\"';/g' nginx.conf && nginx -c $(pwd)/nginx.conf -g 'daemon off;'"
